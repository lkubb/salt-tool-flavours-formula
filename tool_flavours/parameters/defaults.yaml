# vim: ft=yaml
#
# Set default values.
---
values:
  lookup:
    pkg:
      name: flavours
    paths:
      confdir: '.config/flavours'
      conffile: 'config.toml'
      datadir: '.local/share/flavours'
      xdg_dirname: 'flavours'
      xdg_conffile: 'config.toml'
    rootgroup: root
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
